#pragma once

// A binary search tree node type
struct Node {
    int data;     // The data of interest
    Node *left;   // Link to left subtree
    Node *right;  // Link to right subtree
};

